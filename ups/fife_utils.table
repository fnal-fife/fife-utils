FILE=table
Product=fife_utils

Flavor=ANY
QUalifiers=

Action=setup
    ProdDir()
    SetupEnv()
    SetupRequired(ifdhc)
    SetupRequired(sam_web_client)
    SetupRequierd(jobsub_tools)
    PathPrepend(PATH, ${UPS_PROD_DIR}/bin)
